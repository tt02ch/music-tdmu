"use client"

import { ICardInfo } from "@/app/interfaces/ICardInfo";

import { FaPlay } from "react-icons/fa6";

export default function ButtonPlay(props: ICardInfo) {
  const {
    image = "",
    title = "",
    description = ""
  } = props;
}